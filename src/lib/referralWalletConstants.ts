/** INR credited to referrer when a referral is successfully verified (paid + username). */
export const REFERRAL_CREDIT_INR = 25

/** Minimum wallet balance before user can request a withdrawal (matches product copy). */
export const WITHDRAWAL_THRESHOLD_INR = 1500

/** Maximum INR per single withdrawal request. */
export const MAX_WITHDRAWAL_PER_REQUEST_INR = 1500
